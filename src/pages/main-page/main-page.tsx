import React from 'react';
import cn from 'classnames';

import styles from './mainPage.module.scss';

export const MainPage: React.FC = () => {

    return (
        <div className={styles[cn('wrapper')]}>wrapper</div>
    );
};
